# Neonmem Workspace 1.2.9

## Fixed
- A memory file built with its own search model was searched with the bundled one instead, making results near-random; the file's own model is now used, verified identical to the original engine.

---

Windows x64. Installer is unsigned, so SmartScreen warns on first run. Download ~470 MB; the language
models (~5.5 GB) are offered on first launch and written into the install directory.
